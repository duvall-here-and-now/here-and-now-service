# BMM Workflow Status - here-and-now-service
# Generated by workflow-init on 2025-12-11
# Track progress with: /bmad:bmm:workflows:workflow-status

generated: "2025-12-11"
project: "here-and-now-service"
project_description: "Migrate reminder instances from in-memory to Azure Cosmos DB with ARM template provisioning for Dev and Production instances"
selected_track: "method"
field_type: "brownfield"
workflow_path: "method-brownfield.yaml"

# STATUS KEY:
# - required: Must complete to progress
# - recommended: Strongly suggested
# - optional: Can skip
# - conditional: Required if condition met
# - skipped: User chose not to include
# - {file-path}: Completed (shows output location)

workflow_status:
  # Prerequisite - Required for brownfield projects
  prerequisite:
    document-project:
      status: required
      agent: analyst
      command: "/bmad:bmm:workflows:document-project"
      purpose: "Analyze existing codebase and create reference documentation"

  # Phase 0: Discovery (Optional)
  phase_0_discovery:
    research:
      status: requested
      agent: analyst
      command: "/bmad:bmm:workflows:research"
      purpose: "Technical research on Azure Cosmos DB patterns and ARM templates"
    brainstorm:
      status: skipped
    product-brief:
      status: skipped

  # Phase 1: Planning
  phase_1_planning:
    create-prd:
      status: required
      agent: pm
      command: "/bmad:bmm:workflows:create-prd"
      purpose: "Define product requirements for Cosmos DB migration"
    create-ux-design:
      status: conditional
      condition: "if_has_ui"
      agent: ux-designer
      command: "/bmad:bmm:workflows:create-ux-design"

  # Phase 2: Solutioning
  phase_2_solutioning:
    create-architecture:
      status: recommended
      agent: architect
      command: "/bmad:bmm:workflows:create-architecture"
      purpose: "Design integration architecture for Cosmos DB"
    create-epics-and-stories:
      status: required
      agent: pm
      command: "/bmad:bmm:workflows:create-epics-stories"
      purpose: "Break down PRD into implementable stories"
    test-design:
      status: recommended
      agent: tea
      command: "/bmad:bmm:workflows:testarch-test-design"
      purpose: "System-level testability review"
    implementation-readiness:
      status: required
      agent: architect
      command: "/bmad:bmm:workflows:check-implementation-readiness"
      purpose: "Validate all artifacts before implementation"

  # Phase 3: Implementation
  phase_3_implementation:
    sprint-planning:
      status: required
      agent: sm
      command: "/bmad:bmm:workflows:sprint-planning"
      purpose: "Create sprint plan with prioritized stories"
